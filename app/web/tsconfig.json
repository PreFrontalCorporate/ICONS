{
  "compilerOptions": {
    /* ——— language & emit ——— */
    "module": "esnext",
    "jsx": "preserve",
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    /* ——— safety ——— */
    "strict": false,
    "skipLibCheck": true,
    "strictNullChecks": true,
    "isolatedModules": true,
    "noEmit": true,
    "incremental": true,
    /* ——— module resolution & aliases ——— */
    "moduleResolution": "node",
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "baseUrl": ".", // root of the web workspace
    "paths": {
      "@/*": [
        "./*"
      ] // e.g. "@/components/…"
    },
    /* ——— 👇  IMPORTANT  ——— */
    "types": [
      "node"
    ], // keep Node globals
    "typeRoots": [
      "./types", // our local stubs
      "../node_modules/@types", // regular DefinitelyTyped pkgs
      "../../node_modules/@types" // root‑level (monorepo) pkgs
    ],
    /* ——— Next.js plugin ——— */
    "plugins": [
      {
        "name": "next"
      }
    ],
    "allowJs": true
  },
  "include": [
    "next-env.d.ts",
    ".next/types/**/*.ts",
    "**/*.ts",
    "**/*.tsx",
    "types/**/*.d.ts" // ensure the stub is picked up
  ],
  "exclude": [
    "node_modules"
  ]
}
